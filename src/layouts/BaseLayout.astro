---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import SideBar from "../components/SideBar.astro";
import CookieBanner from '../components/CookieBanner.astro';
import presentation from "../data/presentation";




import { SITE_TITLE, SITE_DESCRIPTION } from "../config";

const { image, title = SITE_TITLE, description = SITE_DESCRIPTION, includeSidebar = true, sideBarActiveItemID } = Astro.props;

import { ThemeSwitch } from "astro-color-scheme";

import Swup from 'swup';
---

<script is:inline>
	const getThemePreference = () => {
		if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
			return localStorage.getItem('theme');
		}
		return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
	};
	const isDark = getThemePreference() === 'dark';
	document.documentElement.classList[isDark ? 'add' : 'remove']('dark');
 
	if (typeof localStorage !== 'undefined') {
		const observer = new MutationObserver(() => {
			const isDark = document.documentElement.classList.contains('dark');
			localStorage.setItem('theme', isDark ? 'dark' : 'light');
		});
		observer.observe(document.documentElement, { attributes: true, attributeFilter: ['class'] });
	}
</script>

<!DOCTYPE html>
<html lang="de">
  <head>
    <BaseHead title={title} description={description} image={image} />
    <link rel="sitemap" href="/sitemap-0.xml" />
    <script>import Swup from "swup";

      const swup = new Swup();
    </script>
  </head>
  <body>
    <div class="bg-background drawer lg:drawer-open">
      <input id="my-drawer" type="checkbox" class="drawer-toggle" />
      <div class="drawer-content bg-background">
        <Header title={SITE_TITLE} class="bg-background glass" />
        
        <div class="md:flex md:justify-center">
          <main class="p-6 pt-10 lg:max-w-[900px] max-w-[100vw]">
            <CookieBanner class="glass" />
            <slot />
          </main>
        </div>
        <div class="divider my-0"></div>
        <Footer class="glass" />
      </div>
      {includeSidebar && <SideBar class="" sideBarActiveItemID={sideBarActiveItemID} />}
    </div>
  </body>
</html>